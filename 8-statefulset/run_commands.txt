> kubectl apply -f service.yml (headless service -> no service ip)

|check these pods have persistance storage
 > kubectl get pvc

|go to web-0 pod
> kubectl exec -it web-0 -- sh
> echo "data from web-0" > /data/file.txt

|delete that pod
|go to web-0 pod
|check it get re-create as web-0
|check that file still exist in pod
> kubectl exec -it web-0 -- sh
> cat /data/file.txt


👉 StatefulSet = keeps pod identity + PVC
👉 Headless Service = gives stable DNS per pod
   ex:- backend + db (statefulset) -> stable host name... 
